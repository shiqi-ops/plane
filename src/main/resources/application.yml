spring:
  web:
    resources:
      static-locations: classpath:/static/file/
      add-mappings: true
  application:
     name: plane
  profiles:
    active: dev
  datasource:
    driver-class-name: ${shiqi.datasource.driver-class-name}
    url: jdbc:mysql://${shiqi.datasource.host}:${shiqi.datasource.port}/${shiqi.datasource.database}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    username: ${shiqi.datasource.username}
    password: ${shiqi.datasource.password}
  data:
    redis:
      host: localhost
      port: 6379
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB
  mail:
    default-encoding: utf-8
    host: smtp.qq.com
    port: 465
    username: 3181842264@qq.com
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          socketFactory:
            class: javax.net.ssl.SSLSocketFactory
            fallback: false
        debug: true
    password: kafzxcliwrzbdedg
langchain4j:
  open-ai:
    chat-model:
      base-url: ${shiqi.ai.base-url}
      api-key: ${shiqi.ai.api-key}
      model-name: "qwen-plus"
      log-requests: true
      log-responses: true
    streaming-chat-model:
      base-url: ${shiqi.ai.base-url}
      api-key: ${shiqi.ai.api-key}
      model-name: "qwen-plus"
      log-requests: true
      log-responses: true

jwt:
  secret: "my-super-secret-key-for-plane-project-must-be-long-enough"
  expiration: 3600000
